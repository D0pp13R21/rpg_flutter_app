import 'package:flutter/material.dart';

class TeologiaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Teologia no RPG')),
      body: Center(
        child: Text('Diferentes religiões e divindades para o RPG.'),
      ),
    );
  }
}
